import { action } from '@storybook/addon-actions';

import Button from './button.svelte';

export default {
	title: 'Button',
	component: Button,
};

export const Default = () => ({
	Component: Button,
});

